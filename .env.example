# ===========================================
# Qontak MCP Configuration
# ===========================================
# This file is used for both the MCP server and integration tests.
# Copy this file to .env and customize for your environment.

# ===========================================
# REQUIRED: Qontak API Credentials
# ===========================================
# Get your refresh token from: https://crm.qontak.com/crm/api_token/
QONTAK_REFRESH_TOKEN=your_refresh_token_here

# ===========================================
# Token Store Selection
# ===========================================
# Options:
#   env   - Local development only (single tenant, no caching)
#   redis - Development/Staging (multi-tenant, 6-hour token caching) ⚡ RECOMMENDED
#   vault - Production (multi-tenant, encrypted, audited) ✅ PRODUCTION ONLY
#
# Note: Redis provides 5-10x faster API calls by caching tokens!
TOKEN_STORE=env

# ===========================================
# Redis Configuration (TOKEN_STORE=redis)
# ⚡ Recommended for development/staging
# ===========================================
# Redis connection URL
# REDIS_URL=redis://localhost:6379/0

# Redis key prefix for MCP server tokens
# REDIS_KEY_PREFIX=qontak:tokens:

# Redis key prefix for integration test tokens (different from server)
# REDIS_TEST_KEY_PREFIX=qontak:test:tokens:

# Token TTL in Redis (seconds) - matches Qontak token expiry (6 hours)
# REDIS_TOKEN_TTL=21600

# ===========================================
# Vault Configuration (TOKEN_STORE=vault)
# ✅ Recommended for production deployments
# ===========================================
# VAULT_ADDR=https://vault.example.com:8200
# VAULT_TOKEN=hvs.xxxxxxxxxxxxx
# VAULT_MOUNT_PATH=secret
# VAULT_SECRET_PATH=qontak/tokens
# VAULT_NAMESPACE=  # Optional: for Vault Enterprise

# ===========================================
# Integration Tests (Optional)
# ===========================================
# Uncomment to add delays between test requests (seconds)
# INTEGRATION_TEST_DELAY=0.5
